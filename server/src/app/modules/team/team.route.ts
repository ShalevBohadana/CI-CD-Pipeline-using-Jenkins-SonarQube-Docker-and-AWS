import express, { Router } from 'express';

const router: Router = express.Router();

// Add team routes here
// Example:
// router.get(
//   '/',
//   auth(USER_ROLE_ENUM.ADMIN),
//   TeamController.getTeamMembers
// );

export const TeamRoute = router;
