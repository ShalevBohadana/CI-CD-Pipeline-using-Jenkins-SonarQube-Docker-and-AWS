import express, { Router } from 'express';

const router: Router = express.Router();

// Add review routes here
// Example:
// router.post(
//   '/',
//   auth(USER_ROLE_ENUM.CUSTOMER),
//   ReviewController.createReview
// );

export const ReviewRoute = router;
